[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "epilepsy_prediction"
version = "0.1.0"
description = "Epilepsy Prediction using Machine Learning"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]